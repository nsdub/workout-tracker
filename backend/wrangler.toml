name = "protocol-sync"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV Namespace binding
# Create with: npx wrangler kv:namespace create "PROTOCOL_DATA"
# Then update the id below with the returned ID
[[kv_namespaces]]
binding = "PROTOCOL_DATA"
id = "YOUR_KV_NAMESPACE_ID"
